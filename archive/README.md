# Archived Experiments

This folder holds older exploratory scripts that are useful for reference but are not the current main path.

- `minictf_identical_agents.py`: a simplified symmetric CTF variant where both sides use the same genome and the controller gets an explicit carry-bit sensor.

The canonical stage-2 implementation is now `../mini_ctf.py`.
